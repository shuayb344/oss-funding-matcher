# Scripts

## Seed funders

Loads the starter funder data into your Supabase database.

```bash
# Make sure your .env.local is set up with Supabase credentials
npx tsx scripts/seed-funders.ts
```

Requires `tsx` (already in devDependencies via Next.js).
